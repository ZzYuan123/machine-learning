# 📖 Machine Learning 学习笔记

> 作者：赵忠原
>

---

# 第一章 机器学习概述

## 什么是机器学习

一句话：

> 让计算机从数据中学习规律，而不是人为编写规则。

机器学习主要分为：

- 监督学习
- 无监督学习
- 半监督学习
- 强化学习

类型	In（输入）	Out（输出）	目的	案例
监督学习(supervised learning)	有标签	有反馈	预测结果	猫狗分类、房价预测
无监督学习(unsupervised learning)	无标签	无反馈	发现潜在结构	“物以类聚，人以群分”
半监督学习(Semi-Supervised Learning)	部分有标签，部分无标签	有反馈	降低数据标记的难度	-
强化学习(reinforcement learning)	决策流程及激励系统	一系列行动	长期利益最大化	学下棋

## 常见术语
样本: 一行数据就是一个样本 多个样本组成数据集 有时一条样本被叫成一条记录
特征: 一列数据一个特征 有时也被称为属性
标签/目标: 模型要预测的那一列数据


我们目前学习了：

✅ 监督学习
- 线性回归
- 逻辑回归
- 决策树
- 随机森林
- XGBoost

✅ 无监督学习
- KMeans

---

# 第二章 特征工程

## 特征工程概念
特征工程是: 利用专业背景知识和技巧处理数据 让机器学习算法效果最好 这个过程就是特征工程
ps: 数据和特征决定了机器学习的上限 模型和算法只是逼近这个上限而已

## 特征提取
从原始数据中提取与任务相关的特征 构成特征向量

## 特征预处理
特征对模型产生影响 因量纲问题 有些特征对模型影响很大 有些很小 所以要对特征进行预处理
方法一: 归一化:将数据缩放到 **[0, 1]** 或 **[-1, 1]** 区间。

​	     公式：![1783513478726](C:\Users\29412\AppData\Roaming\Typora\typora-user-images\1783513478726.png) 

​	     特点 ：有界性：输出范围固定（通常[0,1]）
​	                 对异常值敏感：最大值或最小值异常会影响整体
​              适合：数据分布有明确边界、需要压缩到固定区间的场景

方法二: 标准化：将数据转化为**均值为0，标准差为1**的标准正态分布。

​	     公式：![1783513595935](C:\Users\29412\AppData\Roaming\Typora\typora-user-images\1783513595935.png)

​	     特点 ：无界性：输出没有固定范围
​	                 对异常值敏感度相对归一化较小
​              适合：数据服从正态分布、有异常值、需要稳定方差的场景

## 特征降维
将原始数据的维度降低 就叫做特征降维 
特征降维会使一部分信息丢失 原始数据会发生变化 只保留了最主要的信息

## 特征选择
原始数据特征很多 但是对任务相关是其中一个特征集合子集
特征选择不会改变原来的数据

## 特征组合
通过矩阵运算 把不同的特征矩阵重新组合 拼成一个新的矩阵

# 第三章 模型拟合问题
拟合: 用来表示模型对样本点的拟合情况

欠拟合：模型在训练集上表现很差、在测试集表现也很差 原因: 模型过于简单

过拟合: 模型在训练集上表现很好、在测试集表现很差 原因: 模型太过于复杂、数据不纯、训练数据太少

泛化: 模型在新数据集（非训练数据）上的表现好坏的能力
奥卡姆剃刀原则: 给定两个具有相同泛化误差的模型，较简单的模型比较复杂的模型更可取
# 第四章 线性回归（Linear Regression）

## 线性回归概述
用线性公式来描述多个自变量x或一个自变量x和一个因变量y之间的关系
对这个关系进行建模 从而达到特征预测标签
自变量x相当于特征值 因变量y相当于标签

## 分类
1 一元线性回归: 1个特征列 + 1个标签列
2 多元线性回归: 多个特征列 + 1个标签列

## 用途

预测连续值

例如：

- 房价预测
- 工资预测
- 气温预测

## 模型
一元线性回归
y = wx + b w:权重 b:偏置
多元线性回归
y = w1x1 + w2x2 + w3x3 + ... + wnxn -> w的转置 * x + b
## 损失函数
误差 = 预测值 - 真实值
损失函数(Loss Function) 也叫做成本函数(Cost Function) 也叫做代价函数、目标函数
用于描述每个样本点和其预测值之间的关系 尽可能的让损失函数的值变小
也就是让误差变小 这样线性回归的结果评估越好
问题 如何损失函数最小?
答案: 
​    思路1 正规方程法 直接求导 一步求导 直接算导数等于0的时候极小值
​    思路2 梯度下降法
## 损失函数分类
最小二乘法: loss = (y1'-y1)²+(y2'-y2)²
MSE(Mean Squared Error) 均方误差 loss = ((y1'-y1)²+(y2'-y2)²) / 2
RMSE(Root Mean Squared Error) 均方根误差 loss = sqrt (((y1'-y1)²+(y2'-y2)²) / 2)
MAE(Mean Absolute Error) 平均绝对误差 loss = (abs(y1' - y1) + abs(y2' - y2)) / 2

## 梯度下降

目的：

不断更新参数 w 和 b。

公式不用背。

理解：

> 顺着损失下降最快的方向不断更新参数。

## 学习率（Learning Rate）

学习率越大：

容易震荡。

学习率越小：

收敛慢。

---

# 第三章 正则化

为什么需要？

模型容易过拟合。

所以：

需要限制模型复杂度。

## Ridge（L2）

作用：

限制参数不要太大。

特点：

不会变成0。

## Lasso（L1）

作用：

限制参数。

特点：

可以把参数压成0。

可以做特征选择。

---

# 第四章 逻辑回归（Logistic Regression）

用途：

分类。

例如：

垃圾邮件识别。

## 为什么不用线性回归？

因为：

分类结果不是连续值。

所以：

引入：

Sigmoid

作用：

把任何值压缩到：

0~1。

表示概率。

## 判断方式

默认：

> 概率 > 0.5

预测为：

正类。

---

# 第五章 分类指标

## 混淆矩阵

TP

真正例。

FP

假正例。

TN

真负例。

FN

假负例。

## Precision（查准率）

预测正确的正样本比例。

公式：

TP / (TP + FP)

## Recall（召回率）

真实正样本找回来多少。

公式：

TP / (TP + FN)

## F1 Score

Precision 与 Recall 的调和平均。

---

# 第六章 决策树（Decision Tree）

思想：

不断提出问题。

例如：

X <= 2.5？

满足：

左边。

否则：

右边。

## Gini

基尼不纯度。

越小：

分类越纯。

0：

完全纯。

0.5：

最混乱。

## 常见字段

samples

节点样本数量。

value

各类别数量。

class

最终预测类别。

---

# 第七章 决策树剪枝（Pruning）

目的：

防止树无限生长。

降低过拟合。

思想：

收益不够：

停止分裂。

---

# 第八章 随机森林（Random Forest）

思想：

很多棵决策树一起预测。

分类：

投票。

回归：

平均。

特点：

- Bootstrap随机抽样
- 随机选择特征

优点：

降低过拟合。

---

# 第九章 KMeans

属于：

无监督学习。

作用：

自动聚类。

步骤：

① 随机选择K个中心

↓

② 分配最近中心

↓

③ 更新中心

↓

④ 重复直到收敛

---

# 第十章 XGBoost

核心思想：

Boosting。

每棵树：

学习上一棵树的错误。

最终：

不断修正预测。

## Learning Rate

控制：

每次修正多少。

越小：

越稳定。

## Gamma

控制：

是否继续分裂。

越大：

树越简单。

## Lambda

控制：

叶子输出不要太极端。

作用：

防止过拟合。

---

# 第十一章 模型总结

| 模型               | 类型      | 用途               |
| ------------------ | --------- | ------------------ |
| LinearRegression   | 回归      | 连续值预测         |
| Ridge              | 回归      | 防止线性回归过拟合 |
| LogisticRegression | 分类      | 二分类             |
| DecisionTree       | 分类/回归 | 树模型             |
| RandomForest       | 集成学习  | 多树投票           |
| KMeans             | 聚类      | 无监督学习         |
| XGBoost            | Boosting  | 高精度预测         |

---

# 第十二章 学习主线（⭐）

机器学习真正学习的是：

数据

↓

建立模型

↓

计算损失

↓

梯度下降优化

↓

过拟合

↓

正则化

↓

树模型

↓

Bagging（随机森林）

↓

Boosting（XGBoost）

↓

得到泛化能力更强的模型

---

# 我的理解（持续更新）

（这里留空，记录自己的理解和踩坑。）

---

# 面试高频问题

（持续更新。）